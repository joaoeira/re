{
  "name": "re",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "prepare": "effect-language-service patch",
    "test": "bun run --filter '*' test",
    "test:watch": "bun run --filter '*' test:watch",
    "typecheck": "bun run --filter '*' typecheck",
    "desktop:dev": "bun --cwd=apps/desktop run dev",
    "desktop:typecheck": "bun --cwd=apps/desktop run typecheck",
    "desktop:test": "bun --cwd=apps/desktop run test",
    "desktop:build": "bun --cwd=apps/desktop run build",
    "desktop:package": "bun --cwd=apps/desktop run package",
    "desktop:install": "bun --cwd=apps/desktop run install:local",
    "raycast:dev": "bun --cwd=apps/raycast run dev",
    "raycast:test": "bun --cwd=apps/raycast run test",
    "raycast:typecheck": "bun --cwd=apps/raycast run typecheck",
    "raycast:build": "bun --cwd=apps/raycast run build",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check .",
    "build:library": "tsc -b tsconfig.build.json",
    "watch:library": "tsc -b tsconfig.build.json --watch",
    "pack:library": "node scripts/pack-library.mjs",
    "check:packages": "node scripts/check-packages.mjs",
    "check:raycast": "node scripts/check-raycast.mjs",
    "check:desktop": "node scripts/check-desktop.mjs",
    "changeset": "changeset",
    "release:version": "node scripts/prepare-library-release.mjs",
    "release:check": "node scripts/check-library-release.mjs",
    "release:dry-run": "node scripts/publish-library.mjs --dry-run",
    "release:publish": "node scripts/publish-library.mjs --publish",
    "test:release": "node --test scripts/library-release.test.mjs",
    "overlay:dev": "bun --cwd=apps/overlay run dev",
    "overlay:build": "bun --cwd=apps/overlay run build",
    "check:app-resolution": "node scripts/check-app-resolution.mjs && bun scripts/check-app-resolution.mjs",
    "test:app-isolation": "node --test scripts/app-isolation.test.mjs",
    "check:overlay": "node scripts/check-overlay.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.2",
    "@effect/language-service": "0.87.2",
    "@oxlint/plugins": "^1.55.0",
    "@types/bun": "latest",
    "add": "^2.0.6",
    "oxfmt": "^0.33.0",
    "oxlint": "^1.55.0",
    "typescript": "^5.9.3"
  },
  "patchedDependencies": {
    "@gpuix/react@0.7.0": "patches/@gpuix%2Freact@0.7.0.patch"
  }
}

diff --git a/dist/components/floating.js b/dist/components/floating.js
index b04b3f0d0e7ac5aa498075737879ff890ecd159e..86bd9f63859bddec255a41115d8f05b8f2cdeb9e 100644
--- a/dist/components/floating.js
+++ b/dist/components/floating.js
@@ -108,6 +108,13 @@ export const FloatingLayer = forwardRef(function FloatingLayer({ side = "bottom"
     const offset = side === "top" || side === "bottom"
         ? { x: alignOffset, y: 0 }
         : { x: 0, y: alignOffset };
-    return (_jsx("anchored", { side: side, align: align, gap: sideOffset, offset: offset, fit: "snap", snapMargin: collisionPadding, deferred: true, priority: 1, occlude: true, children: _jsx("div", { ...props, ref: ref, style: mergeStyles({ backgroundColor: "#1A1A1A" }, props.style), children: children }) }));
+    // The native anchored element paints its own fallback surface. Match the
+    // rounded child so that backing surface cannot show square corner wedges.
+    const surfaceStyle = {
+        background: props.style?.background,
+        backgroundColor: props.style?.backgroundColor ?? "#1A1A1A",
+        borderRadius: props.style?.borderRadius,
+    };
+    return (_jsx("anchored", { style: surfaceStyle, side: side, align: align, gap: sideOffset, offset: offset, fit: "snap", snapMargin: collisionPadding, deferred: true, priority: 1, occlude: true, children: _jsx("div", { ...props, ref: ref, style: mergeStyles({ backgroundColor: "#1A1A1A" }, props.style), children: children }) }));
 });
 //# sourceMappingURL=floating.js.map
\ No newline at end of file
